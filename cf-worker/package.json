{
  "name": "split-expense-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Split Expense App",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "build": "wrangler publish --dry-run",
    "test": "vitest run"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "typescript",
    "expense",
    "split"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.2.0",
    "@cloudflare/workers-types": "^4.20240403.0",
    "typescript": "^5.3.3",
    "vitest": "^1.5.0",
    "wrangler": "^3.57.0"
  },
  "dependencies": {
    "@types/node": "^20.19.6",
    "hono": "^4.4.12"
  }
}
